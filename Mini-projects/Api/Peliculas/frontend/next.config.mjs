/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['plum-used-toad-933.mypinata.cloud','image.tmdb.org'], // Agrega aquí tu dominio
    },
  };

export default nextConfig;
